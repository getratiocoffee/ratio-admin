# Ratio Coffee App — 本次變更說明（2026-06-24）

你休息時，我只做了「純加法、不改外觀與行為」的穩定性強化，沒有動結構。
全部以 `node --check` 驗證語法、檢查 `<div>` 平衡，並在瀏覽器用測試套件驗證新邏輯（全部通過）。

## 檔案
- `index.html` — 更新後的主檔（可直接上線）。
- `index.backup-2026-06-24.html` — 動工前的「可運作備份」。萬一有任何異常，直接用這支還原即可。

## 做了什麼

1. **儲存重試（讓儲存更可靠）**
   - 所有讀寫都走同一個入口 `apiRequest`，我在這裡加了「暫時性失敗自動重試」：
     - 網路錯誤或伺服器 5xx → 自動重試（最多 3 次，間隔 0.5s、1s）。
     - 401（未授權）→ 立即停止並走原本的重新登入流程（未更動）。
     - 4xx（用戶端錯誤）→ 不重試（重試也沒用）。
   - 因為儲存是「整包資料覆蓋寫入」，重送是安全的、不會產生重複或半筆資料。
   - 成功/失敗仍沿用你原本的 toast 提示。

2. **錯誤隔離（分散壞掉的可能性）**
   - 加了全域錯誤攔截（`window.onerror` / `unhandledrejection`）：把錯誤靜默記錄到本機（最多保留最後 50 筆），**不會跳擾人的視窗、也不會嚇到店員**。
   - `showScreen` 的每個畫面初始化都包了 try/catch：就算某一頁初始化出錯，畫面仍會正常切換、**不會整頁白掉**，其他頁也不受影響。

3. **資料匯出／備份（儲存有效的最後保險）**
   - 在「Setting」頁最下方新增「Data Backup → Export backup (.json)」按鈕。
   - 一鍵把目前所有資料（生豆、聯絡人、轉移、發票、員工、班表、N/A、杯測、熟豆、交接）連同錯誤紀錄，下載成一個 JSON 檔。
   - **客戶個資（customers）刻意排除**，不寫進本機備份檔（依你的隱私要求）。客戶資料請繼續以伺服器端為準／另行備份。

## 驗證結果（全部通過）
- 重試：網路連兩次失敗後第三次成功 → 成功；401/4xx → 立即停止不重試；5xx → 重試 3 次後回報失敗；正常時一次成功。
- 錯誤紀錄：寫入 60 筆後只保留最後 50 筆（第 11～60 筆）。
- 匯出：輸出不含 customers 欄位、客戶姓名不外洩、其餘 10 類資料都在。

## 沒有動的部分（等你回來再一起做）
- **簡化 index、把功能拆模組**：高風險、需逐步搬移＋每步驗證，建議我們一起做（搬一個、驗一個）。
- **本機優先儲存＋離線佇列（PWA / Service Worker）**：價值高，但牽涉後端語意與離線同步衝突規則，需要你確認後再做。

## 回來後待確認（會影響後續做法）
1. **Build**：之後重構能否接受一個很小的 build（開發模組化、上線仍是單一 `index.html`），還是要永遠維持「純單一檔、零 build」？
2. **後端**：`/api/beans`、`/api/transfer-auth` 是你能改的嗎？還是繼續當黑盒、只在前端強化？
3. **離線策略**：要不要做 PWA（網路不穩也能開、能用，回線再同步）？
4. **錯誤紀錄查看**：目前錯誤記在本機、會一起包進匯出檔。要不要再加一個「畫面上直接看錯誤紀錄」的小入口？

## 建議的下一步（風險低→高）
1. （已完成）錯誤隔離 ＋ 儲存重試 ＋ 匯出備份。
2. 加 PWA 離線能力（需你拍板）。
3. 用「試點＋逐一搬移」把第一個功能拆出來，驗證流程順了再繼續。
