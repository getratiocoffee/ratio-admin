# Green Bean — C Market 圖表 ＋ 多維度分析（2026-06-24）

你休息時做的兩個功能，都是純加法、沒有改動既有資料或流程，並在瀏覽器用實際程式碼＋樣本資料逐項驗證過（含數字手算核對）。

## 檔案
- `index.html` — 更新後主檔（含先前的穩定性強化 ＋ 這次兩個新功能）。
- `index.backup-2026-06-24b-features.html` — 這個版本的備份。
- `index.backup-2026-06-24.html` — 今天所有改動「之前」的乾淨還原點。

---

## 1) C Market 走勢圖（新按鈕）

- 在 Green Bean 選單新增一張卡片 **「C Market」**（Inventory／Purchase／Stock Transfer 下面），點進去是專屬的咖啡 C 期貨價走勢畫面。
- 畫面內容：
  - 目前價（US¢/lb）、區間漲跌（▲/▼、百分比，綠漲紅跌）。
  - 時間範圍切換：**1M / 3M / 6M / 1Y**。
  - 面積＋折線圖（含 Y 軸刻度、起訖日期、最新點標記）。
  - 區間統計：**High / Low / Average**。
  - 一段說明：C 價是 arabica 全球基準，多數生豆合約以「C ＋ 差價」計價。

### ⚠️ 重要：目前是「範例資料」
畫面右上有明顯的 **「SAMPLE DATA」** 標籤。圖上的數字是**示意用的假資料**（用固定種子產生、每次重開都一樣，純粹給版面與互動用），**不可拿來做實際買賣判斷**。

### 怎麼接真實價格（之後）
程式裡留了**單一接口** `loadCMarketData(range)`。要顯示真實價時，只要把這個函式的內容換成「回傳 `[{t:時間戳, v:價格}, …]`」即可，其餘圖表/統計/切換都會自動運作。資料來源可選：
- 你自己的後端（例如多開一個 `/api/cmarket?range=…`），或
- 外部行情 API（多半需要金鑰；用前端直接打可能有 CORS 限制，通常建議由後端代抓）。
回來後告訴我你想用哪一種，我幫你接上。

---

## 2) Green Bean 圓餅圖 → 多維度分析

原本只有「依國別」的圓餅圖，現在升級成可切換的分析面板（在 Green Bean 首頁上方）：

- **頂部統計列**：Lots（筆數）、Total weight（總重 kg）、Inventory value（庫存總值 $）、Avg cost（加權平均成本 $/kg）。
- **Break down by（維度切換）**：Country 國別 / Variety 品種 / Process 處理法 / Supplier 供應商。
- **Measure（指標切換）**：By weight (kg) 依重量 / By value ($) 依金額。
- 圓餅圖與圖例會依「維度 × 指標」即時重算，圖例同時顯示每項的百分比與實際數值（kg 或 $）。

資料來源就是你現有的生豆庫存欄位（country / variety / process / supplier / quantity / costPerKg），不需要另外輸入。空白欄位會歸到「Unspecified」。

### 驗證（手算核對無誤）
- 樣本 5 筆：總重 171.5kg、總值 $2,794.10、均價 $16.29/kg。
- 依品種×金額：Mundo Novo $1,033.60 (37%)、Heirloom $756.00 (27%)、Caturra $412.50 (15%)、Tim Tim $384.00 (14%)、Geisha $208.00 (7%)。
- 維度切換、指標切換都即時更新且數字正確。

---

## 沒有動到的部分
- 既有的庫存、入庫、轉移、儲存與重新登入流程都未更動。
- `renderCountryBreakdown` 仍沿用原名（庫存清單頁也會呼叫它），相容。

## 回來後可決定的小事
1. C Market 要接哪種真實資料來源（你的後端 / 外部 API）？
2. 分析面板要不要再加維度（例如：產季 harvest、處理站 station）或「平均成本 by 維度」的小洞察？
3. 圓餅圖項目很多時要不要併成「其他」一類，避免圖例過長？
